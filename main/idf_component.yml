# ================ IDF Component Manager ================
# 自动拉取 LVGL 9.5.0 (主工程的 lcd_st7735 驱动是自写的)

dependencies:
  lvgl/lvgl:
    version: "9.5.0"
  # 注意: MicroPython 默认 STUB 模式（编译快, 桌面仍能跑）
  # 想真跑 .app 时:
  #   1. 取消下面注释
  #   2. 在 sdkconfig.defaults 加 CONFIG_ENABLE_MICROPYTHON=y
  #   3. 注意 micropython 组件需要 ~300KB Flash
  # micropython/micropython:
  #   version: "^1.23"